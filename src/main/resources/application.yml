server:
  port: 3000

spring:
  datasource:
    url: jdbc:h2:mem:tokendb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  h2:
    console:
      enabled: true

springdoc:
  api-docs:
    path: /v3/api-docs

